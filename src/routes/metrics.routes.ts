import { FastifyRequest, FastifyReply } from 'fastify';
import { logger } from '../config/logger.js';

/**
 * Metrics Routes - Placeholder for future implementation
 * TODO: Complete implementation with proper MetricsService integration
 */

export async function registerMetricsRoutes(fastify: any) {
  // Routes are not yet registered - to be implemented in future
  logger.info('Metrics routes registered (placeholder)');
}
